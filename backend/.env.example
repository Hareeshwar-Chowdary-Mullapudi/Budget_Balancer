PORT=5000
MONGO_URI=mongodb+srv://harimullapudi:Hari@cluster0.svsvehy.mongodb.net/?appName=Cluster0
JWT_SECRET=change_this_to_a_long_random_secret_string
JWT_EXPIRES_IN=7d

# Comma-separated frontend URLs allowed by CORS (required in production when
# frontend and API are on different domains).
# Example: https://your-app.netlify.app,https://www.yourdomain.com
CLIENT_ORIGIN=

# Set to production on your host (Render, Railway, etc.)
NODE_ENV=development

# Backend API origin (no trailing slash). Leave empty when the API is on the
# same host as the frontend (e.g. /api proxy or reverse proxy).
# Example for split deploy: https://your-api.onrender.com
VITE_API_URL=
